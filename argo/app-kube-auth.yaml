apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kube-auth
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://your.git.host/your-user/kube-auth.git
    path: .
    targetRevision: main
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.namespaces.argocd | default "argocd" }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

